import React from 'react';

const ProductItem = (props) => {
    const {imageUrl, name, count, size, weight, comments} = props.data;
    return (
        <div>
            
        </div>
    );
};

export default ProductItem;